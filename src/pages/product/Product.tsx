import Single from '../../components/single/Single';

import './product.scss';

const Product = () => {
  return (
    <section>
      <Single />
    </section>
  );
};

export default Product;
